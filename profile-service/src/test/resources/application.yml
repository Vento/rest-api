# ===================================================================
# Profile Service test configuration
# ===================================================================

# In-memory MongoDB with random port at startup for testing
spring:
  data:
    mongodb:
      database: vento_profiletestdb
      port: 0

camel:
  endpoint:
    auth:
      producerUri: "rabbitmq://vento.service.profile/users"
      consumerUri: "rabbitmq://vento.service.auth/users"