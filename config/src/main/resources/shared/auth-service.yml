# ===================================================================
# Auth Service configuration
# ===================================================================

server:
  context-path: /uaa
  port: 3000

spring:
  data:
    mongodb:
      database: test
---
spring:
  profiles: production
  data:
    mongodb:
      host: auth-db
      database: vento
      port: 27017